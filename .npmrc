# Canopy is a public open-source project: resolve every dependency from the
# public npm registry. Without this, a machine configured for a private
# corporate registry cannot install this repo — which would break the
# cold-clone guarantee for anyone cloning it, including a judge.
registry=https://registry.npmjs.org/
audit=false
fund=false
